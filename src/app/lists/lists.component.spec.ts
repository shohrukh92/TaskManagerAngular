import { TestBed } from '@angular/core/testing';
import { ListsComponent } from './lists.component';

describe('Lists Component Unit Tests', () => {
    describe('1st tests', () => {
        it('true is true', () => expect(true).toBe(true));
    });
});